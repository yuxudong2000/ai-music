# exp-03 验证结果

> **日期**：2026-03-17  
> **状态**：⚠️ 可用（音量已修复，但旋律偏移）

## 修复验证

| 音轨 | 修复前（dBFS） | 修复后（dBFS） | 补偿量 |
|------|-------------|-------------|-------|
| 转换人声 | -40.0 | **-16.3** | +23.8 dB |
| 原始人声（参考） | -16.3 | -16.3 | — |

音量失衡问题已解决，混合后人声与伴奏比例正常。

## 遗留问题

- VC 模式（f0-condition=False）不保留 F0 旋律，歌唱场景下旋律走向仍有偏移  
  → 在 exp-04 中切换为 SVC 模式（f0-condition=True）解决
- 音色与目标（邓丽君）相似度一般（⭐⭐ / 5）

## 结论

音量修复方案（RMS 匹配）有效，`mix_real.py` 的 `match_rms()` 函数纳入标准流程。  
VC 模式不适合歌唱场景，需切换 SVC 模式。
